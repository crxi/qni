---
/**
 * /case-studies/oxford-main — Oxford trapped-ion distributed-compute
 * demonstration. Main et al., Nature (2025), distributed quantum
 * computing across two trapped-ion modules connected by a ~2 m optical
 * network link, with a deterministic teleported CNOT and Grover's
 * algorithm executed across the modules.
 */
import Infographic from "../../layouts/Infographic.astro";
import Center from "../../components/Center.astro";
import Stack from "../../components/Stack.astro";
import SiteNav from "../../components/SiteNav.astro";
import SourceNote from "../../components/SourceNote.astro";
import { url } from "../../lib/url";

const sections = [
  { id: "overview", label: "Overview" },
  { id: "what",     label: "What it is" },
  { id: "verified", label: "Verified claims" },
  { id: "caveats",  label: "Things to note" },
  { id: "related",  label: "Related entries" },
];
---

<Infographic
  title="Oxford trapped-ion distributed-compute demonstration"
  description="Two trapped-ion modules connected by a ~2 m optical network link, with a deterministic teleported CNOT between modules and a two-qubit Grover's search executed across them. Mixed-species architecture: ⁸⁸Sr⁺ network qubit interfaces with photons, ⁴³Ca⁺ circuit qubit holds computational state. Main et al., Nature (2025)."
  references={[
    "main-nature-2025",
  ]}
>
  <SiteNav slot="nav" currentSubject="case-studies" sections={sections} />

  <Center max="var(--wide-width)" gutter="var(--space-3)">
    <Stack space="var(--space-6)">

      <header class="hero" id="overview" data-nav-section>
        <h1>Oxford trapped-ion distributed-compute demonstration</h1>
        <p>
          Two trapped-ion modules separated by ~2 m of optical fibre, with
          heralded ion–ion entanglement used as the resource for a
          deterministic teleported CNOT gate between the two modules' circuit
          qubits. A two-qubit Grover's search was executed across the link,
          making this the first distributed quantum algorithm run across
          physically separate quantum-computing modules.
        </p>
        <dl class="hero-meta">
          <div><dt>Operator</dt><dd>University of Oxford (Lucas group) on lab-bench fibre</dd></div>
          <div><dt>Location</dt><dd>Oxford, UK (single laboratory)</dd></div>
          <div><dt>Year</dt><dd>Published February 2025</dd></div>
          <div><dt>Technology</dt><dd>Entanglement-based; mixed-species trapped ions — ⁸⁸Sr⁺ as the network qubit (photonic interface) and ⁴³Ca⁺ as the circuit qubit (computational state); midpoint beam-splitter Bell-state measurement for heralded ion–ion entanglement</dd></div>
          <div><dt>Scale</dt><dd>Two modules; ~2 m optical link between them</dd></div>
          <div><dt>Status</dt><dd>Research demonstration</dd></div>
          <div><dt>Commercial model</dt><dd>Academic / EPSRC research; not open to outside users</dd></div>
        </dl>
      </header>

      <section id="what" data-nav-section>
        <h2>What it is</h2>
        <p>
          Each module is an ion trap holding two species of ion. A ⁸⁸Sr⁺ ion
          acts as the <em>network qubit</em>: its optical transition is matched
          to the photonic interface, so it can emit a photon entangled with its
          internal state. A ⁴³Ca⁺ ion in the same trap acts as the
          <em>circuit qubit</em>: it carries the computational state, supports
          long-lived coherence, and is coupled to the ⁸⁸Sr⁺ network qubit via a
          local two-qubit gate inside the module.<SourceNote id="main-nature-2025" />
        </p>
        <p>
          To produce a Bell pair between the two modules, each ⁸⁸Sr⁺ ion emits
          a photon entangled with its spin state; the two photons interfere on
          a beam splitter midway between the modules, and a coincident
          detection projects the two ⁸⁸Sr⁺ ions into a Bell pair (the
          Barrett-Kok-style heralding protocol). Each module then swaps that
          entanglement onto its ⁴³Ca⁺ circuit qubit through the local
          inter-species gate, leaving a Bell pair between the two computational
          qubits. Consuming that Bell pair together with two bits of classical
          communication implements a deterministic CNOT between the two
          circuit qubits — gate teleportation in the sense of Gottesman and
          Chuang.<SourceNote id="main-nature-2025" /></p>
        <p>
          With the inter-module CNOT in hand, the team ran a two-qubit instance
          of Grover's search distributed across the two modules. The oracle
          and the diffusion step both require entangling operations between
          the two circuit qubits, and those are supplied by the teleported
          CNOT rather than by a single shared trap. The end-to-end algorithm
          succeeds with the success probability expected from the constituent
          gate fidelities, demonstrating that the network link can carry a
          full algorithmic workload, not just one isolated entangling
          operation.
        </p>
      </section>

      <section id="verified" data-nav-section>
        <h2>Verified claims</h2>
        <ul class="claim-list">
          <li><strong>Two trapped-ion modules connected by a ~2 m optical link</strong>, with heralded photonic entanglement between the two modules' ⁸⁸Sr⁺ network qubits.<SourceNote id="main-nature-2025" /></li>
          <li><strong>Mixed-species architecture per module</strong> — ⁸⁸Sr⁺ as the photonic-interface (network) qubit, ⁴³Ca⁺ as the long-coherence circuit qubit, with a local inter-species gate connecting them.<SourceNote id="main-nature-2025" /></li>
          <li><strong>Deterministic teleported CNOT between the two ⁴³Ca⁺ circuit qubits</strong>, with reported gate fidelity ~86 %.<SourceNote id="main-nature-2025" /></li>
          <li><strong>Two-qubit Grover's search executed across the two modules</strong>, with the inter-module entangling step supplied by the teleported CNOT.<SourceNote id="main-nature-2025" /></li>
          <li><strong>First distributed quantum algorithm executed across two physically separate quantum-computing modules</strong> — the entanglement link is consumed to perform algorithmic gates, not just to certify a Bell pair.<SourceNote id="main-nature-2025" /></li>
        </ul>
      </section>

      <section id="caveats" data-nav-section>
        <h2>Things to note</h2>
        <ul class="caveat-list">
          <li><strong>~2 m link inside a single laboratory.</strong> The two modules sit in the same room, connected by a short free-space / fibre optical path. Loss, dispersion, and phase drift over a deployed metro fibre are outside the scope of this demonstration.</li>
          <li><strong>~86 % teleported-CNOT fidelity.</strong> Fault-tolerant distributed computation will need this gate well above an error-correction threshold; the present fidelity establishes the primitive rather than a production-level operation.</li>
          <li><strong>Each module is a full ion-trap apparatus.</strong> Vacuum chambers, laser racks, control electronics, and imaging optics occupy room-scale benches. Packaging the modules into rack-scale network nodes is separate engineering work.</li>
          <li><strong>Distinct from Knaut and Pompili.</strong> The Boston SiV loop (Knaut et al., 2024) and the Delft three-node NV network (Pompili et al., 2021) entangle matter qubits across a link but do not consume that entanglement to teleport a gate between two computational processors. The Oxford demonstration closes that loop end-to-end.</li>
          <li><strong>Scope of the demonstration.</strong> One link, two modules, one laboratory. The work shows the gate-teleportation primitive that a future distributed quantum computer would rely on, rather than a multi-user network.</li>
        </ul>
      </section>

      <section id="related" data-nav-section>
        <h2>Related entries</h2>
        <ul class="related-list">
          <li><a href={url("/teleportation")}>Teleportation</a> — the gate-teleportation primitive that turns a shared Bell pair plus classical communication into a remote CNOT.</li>
          <li><a href={url("/distribution")}>Entanglement distribution</a> — MeetInTheMiddle architecture with a midpoint beam-splitter Bell-state measurement; the protocol used here to entangle the two ⁸⁸Sr⁺ network qubits.</li>
          <li><a href={url("/repeaters")}>Quantum repeaters</a> — the mixed-species "network qubit + circuit qubit" pattern is the same architecture future 1G / 2G repeater nodes will use.</li>
          <li><a href={url("/case-studies/knaut-boston")}>Harvard SiV Boston-metro loop</a> — matter-qubit entanglement over deployed fibre, without the teleported-gate step.</li>
          <li><a href={url("/case-studies/delft-three-node")}>Delft NV three-node network</a> — academic three-node entanglement + swapping reference; sibling matter-qubit demonstration.</li>
          <li><a href={url("/case-studies/cisco-qunnect-brooklyn")}>Cisco × Qunnect Brooklyn–Manhattan</a> — entanglement distribution over deployed metro fibre, without quantum memory or teleported gates.</li>
          <li><a href={url("/companies")}>Companies</a> — Oxford Ionics and Universal Quantum sit nearby in the trapped-ion vendor landscape.</li>
        </ul>
      </section>

    </Stack>
  </Center>
</Infographic>

<style>
  .hero { padding-block: var(--space-6) var(--space-4); border-block-end: 1px solid var(--color-rule); display: grid; gap: var(--space-3); }
  .hero h1 { margin: 0; }
  .hero-meta { display: grid; gap: var(--space-1); margin: 0; padding: var(--space-3); background: var(--color-surface-2); border-radius: var(--radius); font-size: var(--text-sm); }
  .hero-meta div { display: grid; grid-template-columns: 11rem 1fr; gap: var(--space-2); }
  .hero-meta dt { color: var(--color-text-mute); text-transform: uppercase; letter-spacing: var(--tracking-wide); font-size: var(--text-xs); font-weight: 600; align-self: center; }
  .hero-meta dd { margin: 0; color: var(--color-text); }
  @media (max-width: 40rem) { .hero-meta div { grid-template-columns: 1fr; gap: 0; } }
  .claim-list, .caveat-list, .related-list { display: grid; gap: var(--space-2); padding-inline-start: var(--space-4); }
  .claim-list li, .caveat-list li { line-height: var(--leading-body); }
</style>
