# Build outputs
web/dist/
web/.astro/

# Pagefind index — generated by `npm run build`, mirrored into public/
# for the dev server to serve. Source of truth is the build, not git.
web/public/pagefind/

# Dependencies
node_modules/

# OS
.DS_Store

# Editor
*.swp
*~

# Python (subject render pipelines)
__pycache__/
*.pyc

# Cloudflare Wrangler local state/cache (created by `wrangler pages deploy`)
.wrangler/
